{% extends 'base.html.twig' %}

{% block title %} All articles {% endblock %}

{% block body %}
{# TODO: change presentation of articles #}
    <div class="container">
        <div class="row mx-auto">
            {% for article in articles %}
                <div class="col-md-6 article-container m-2">
                    <div class="col-3 p-0">
                        <img src="/build/images/article/{{ article.imageFileName }}" class="articleImg">
                    </div>
                    <div class="col-7">
                        <a href="{{ path('article', {slug: article.slug}) }}">
                            <span>{{ article.title }}</span>
                        </a>
                    </div>
                    <div class="col-2 p-0">
                        <span>{{ article.publishedAt|ago }}</span>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}